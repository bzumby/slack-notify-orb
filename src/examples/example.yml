description: >
  Example usage for slack-notify-orb

usage:
  version: 2.1
  orbs:
    slack: zumby/slack-notify-orb@0.0.1
  workflows:
    use-my-orb:
      jobs:
        - slack/notify:
            event: pass
            channel: XXXXXXXX
            template: basic_job_success
